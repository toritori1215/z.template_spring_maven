<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itwill.hotel.mapper.BlogMapper">
	
	<!-- Blog -->
	<select id="selectAllBlog" resultType="Blog">
		select bNo, bTitle, bDate, bContent, bReadCount, bCategory, mNo
		from blog
		order by bDate desc
	</select>
	
	<select id="selectOneBlog" resultType="Blog">
		select bNo, bTitle, bDate, bContent, bReadCount, bCategory, mNo
		from blog
		where bNo=#{bNo}
	</select>
	
	<insert id="insertBlog" parameterType="java.util.HashMap">
		insert into blog(bNO, bTitle, bDate, bContent, bReadCount, bCategory, mNo) 
		values (blog_SEQ.nextval, #{bTitle}, sysdate, #{bContent}, 0, #{bCategory}, #{mNo})
	</insert>
	
	<update id="updateBlog" parameterType="java.util.HashMap">
		update blog
		set bTitle=#{bTitle}, bContent=#{bContent}, bDate=sysdate
		where bNo=#{bNo}
	</update>
	
	<delete id="deleteBlog" parameterType="_int">
		delete from blog
		where bNo=#{bNo}
	</delete>
	
	
	<!-- Blog Review -->
	<select id="selectBlogReview" parameterType="_int" resultType="BlogReview">
		select brNo, brDate, brContent, brGroup, brStep, brDepth, mNo, bNo
		from blog_review
		where bNo=#{bNo}
		order by brStep, brDepth, brDate desc
	</select>
	
	<insert id="insertBlogReviewDepth1" parameterType="java.util.HashMap">
		insert into blog_review(brNo, brDate, brContent, brStep, brGroup, brDepth, mNo)
		values (blog_review_SEQ.nextval, sysdate, #{brContent}, 1, #{brGroup}, 1, #{mNo})
	</insert>
	
	<insert id="insertBlogReviewDepth2" parameterType="java.util.HashMap">
		insert into blog_review(brNo, brDate, brContent, brStep, brGroup, brDepth, mNo)
		values (blog_review_SEQ.nextval, sysdate, #{brContent}, #{brStep}, #{brGroup}, 2, #{mNo})
	</insert>
	
	<insert id="insertBlogReviewDepth3" parameterType="java.util.HashMap">
		insert into blog_review(brNo, brDate, brContent, brStep, brGroup, brDepth, mNo)
		values (blog_review_SEQ.nextval, sysdate, #{brContent}, #{brStep}, #{brGroup}, 3, #{mNo})
	</insert>
	
	<update id="updateBlogReview" parameterType="java.util.HashMap">
		update blog_review 
		set brDate=sysdate, brContent, brStep, brGroup, brDepth, mNo 
		where brNo=#{brNo}
	</update>
	
	<delete id="deleteBlogReview" parameterType="_int">
		delete from blog_review
		where brNo=#{brNo} 
	</delete>
</mapper>