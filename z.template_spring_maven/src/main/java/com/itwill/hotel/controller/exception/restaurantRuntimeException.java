package com.itwill.hotel.controller.exception;

import org.springframework.web.bind.annotation.ControllerAdvice;
import org.springframework.web.bind.annotation.ExceptionHandler;

/*
@ControllerAdvice
public class restaurantRuntimeException {
	
	@ExceptionHandler(IllegalArgumentException.class)
	public String handle_IllegalArgumentException(IllegalArgumentException e) {
		 System.out.println(e);
		 return "common_404";
	}
	
	@ExceptionHandler(RuntimeException.class)
	public String handle_RuntimeException(RuntimeException e) {
		System.out.println(e);
		return "common_404";
	}

	
	
	
}
*/
